# Apps

Reserved for self-contained app-level Flutter/mobile projects.

Current app roots:

- `openbot-mobile-control/` - existing mobile app
- `apps/robot_app/` - imported robot-side app

Keep app code here self-contained and do not mix it into `openbene_sdk/`.
New apps should land under `apps/<name>/`.
