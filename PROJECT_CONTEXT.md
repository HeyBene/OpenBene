# OpenBene Project Context

This file is a compatibility pointer.

Canonical architecture and newcomer guidance:

- [docs/architecture.md](docs/architecture.md)

Current stable surfaces:

- `openbene_sdk/` for PC-side Python control
- `openbot-mobile-control/` for the current Flutter mobile app
- `apps/robot_app/` for the imported robot-side Flutter app
- `openbot/` for the firmware baseline

Core rule:

- Phone as body, PC as brain.
